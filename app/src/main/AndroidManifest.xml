<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shohiebsense.constraintlayoutexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CollapseToolbarActivity" />
        <activity android:name=".FormExampleActivity" />
        <activity android:name=".TabLayoutActivity" />
        <activity android:name=".VerificationExampleActivity" />
        <activity android:name=".ScanQrCodeActivity" />
        <activity android:name=".HalfColoredBackgroundActivity" />
        <activity android:name=".ItemExampleActivity" />
        <activity android:name=".Item2ExampleActivity" />
        <activity android:name=".Item3ExampleActivity" />
        <activity android:name=".ExpandableItemActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".ContactsActivity"></activity>
    </application>

</manifest>